\documentclass[11pt]{standalone}	% Required - Tells LaTeX how to display the document.

\title{Standalone}
\date{May 30th, 2018}
\author{Daniel Borrus}

%% Preamble %%

	\usepackage{inputenc}	% Prepares LaTeX engine for non-ascii characters, such as UTF-8 characters. Important.

	% Page Layout
	%\usepackage{geometry} % set page width
	%\usepackage[margin=1cm]{caption} % set caption width
	%\usepackage{float} % Needed for creating figure “floats”

	% Page Display
	\usepackage{url} % Allows for url links
	\usepackage{graphicx} % Allows for inputing figures
	\usepackage[dvipsnames]{xcolor} % For pretty colors

	% Math Typesetting
	\usepackage{amsmath} % Provides useful equation formats
	\usepackage{amssymb} % Provieds a ton of useful math symbols
	\usepackage{mathtools} % Provides patches for amsmath

	% TikZ/pgfPlots
	\usepackage{tikz}
	\usepackage{pgfplots}
	%\pgfplotsset{compat=1.14}
	\usetikzlibrary{automata,arrows,positioning,calc}
	\pgfplotsset{ every non boxed x axis/.append style={x axis line style=-}, every non boxed y axis/.append style={y axis line style=-}}


\begin{document}

%\maketitle

%\begin{figure}[h!]
	\centering
	\begin{tikzpicture}
	
		\pgfmathsetmacro{\xmin}{1998}
		\pgfmathsetmacro{\xmax}{2027}
%		\pgfmathsetmacro{\myh}{2}
%		\pgfmathsetmacro{\myxs}{1.5}
%		\pgfmathsetmacro{\myys}{0}

		\begin{axis}[
			height= 2.5cm,
			width = 8cm,
			yshift = 2.00cm,
			ytick=-1, xtick=-1, 
			extra y ticks = {0.9,10},
			extra y tick labels ={,},
			ymin = 0.9, ymax=10, xmin = \xmin ,xmax=\xmax,
			axis x line = bottom,
			axis y line = left,
			x axis line style = white,	
			ylabel = $a$,
			y label style = {rotate=-90,xshift=1cm},
			]
			\addplot[blue,thick] table[mark=none,y index=1] {../data/data_A.dat};
			%\addplot[black] coordinates {(2023,8)(2025,8)} ;%node[below] {2 s} ;		
		\end{axis}
		
		\begin{axis}[
			height= 2.5cm,
			width = 8cm,
			yshift = 1cm,
			ytick=-1, xtick=-1, xmin = \xmin, xmax=\xmax,
			extra y ticks = {0.5,1.05},
			extra y tick labels ={,},
			ymin = 0.5, ymax=1.05,
			axis x line = bottom,
			axis y line = left,
			x axis line style = white,	
			ylabel = $s$,
			y label style = {rotate=-90,xshift=1cm},			
			]
			\addplot[RedViolet,thick] table[mark=none,y index=2] {../data/data_A.dat};			
		\end{axis}
		
		\begin{axis}[
			height= 2.5cm,
			width = 8cm,
			yshift = -0.25cm,
			ytick=-1, xtick= -1,
			extra x ticks = {2000,2025},
			extra x tick labels = {0 s, 25 s},
			ymin = 2, ymax=8, xmin = \xmin ,xmax=\xmax,
			axis x line = bottom,
			axis y line = left,
			axis line style = white,		
			]
			\addplot[black,thick] coordinates {(2000,2) (2025,2)};						
		\end{axis}
		
		\begin{axis}[
			height= 2.5cm,
			width = 8cm,
			yshift = 0cm,
			ytick=-1, xtick= -1,
			extra y ticks = {2,8},
			extra y tick labels ={,},
			%extra x ticks = {2000,2025},
			%extra x tick labels = {0 s, 25 s},
			ymin = 2, ymax=8, xmin = \xmin ,xmax=\xmax,
			axis x line = bottom,
			axis y line = left,
			x axis line style = white,	
			ylabel = $\theta$,
			y label style = {rotate=-90,xshift=1cm},			
			]
			\addplot[PineGreen,thick] table[mark=none,y index=3] {../data/data_A.dat};
			%\addplot[black,thick] coordinates {(2000,2) (2025,2)};						
		\end{axis}
		

	\end{tikzpicture}

\end{document}